  @BeforeEach
  public void setup(TestInfo testInfo) throws Exception {
    UnitTest.setupTest(testInfo.getDisplayName());
  }

  private String config(Pos carreteraDimensions) {
    int segmentos = carreteraDimensions.getSegmento();
    int carriles = carreteraDimensions.getCarril();
    return "segmentos="+segmentos+","+"carriles="+carriles;
  }
}
